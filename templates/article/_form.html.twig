{{ form_start(form, { 'action': path('upload_file',{'id':album.id}), 'attr': {'id': 'formdrop','class': 'mt-3 dropzone', 'data-controller': 'dropzone'}}) }}
{{ form_errors(form) }}
{{ form_widget(form) }}
<button class="btn btn-primary mr-1 mb-2" type="submit"><i class="ti ti-device-floppy" aria-hidden="true"></i>
    Finish
</button>
{{ form_end(form) }}