{% extends '@AcMarchePresse/base.html.twig' %}

{% block title %}{{ destinataire }}{% endblock %}

{% block body %}

    <div class="card mb-3">
        <div class="card-header">
            <h2>{{ destinataire.nom|upper }} {{ destinataire.prenom }}</h2>
        </div>
        <div class="card-body">
            {% include '@AcMarchePresse/destinataire/_actions.html.twig' %}

            <table class="table table-bordered">
                <tr>
                    <th>Email</th>
                    <td>
                        {{ destinataire.email }}
                    </td>
                </tr>
                <tr>
                    <th>Pièce jointe</th>
                    <td>
                        {% if destinataire.attachment %}
                            Oui
                        {% else %}
                            Non
                        {% endif %}
                    </td>
                </tr>
                <tr>
                    <th>Recevoir la revue de presse</th>
                    <td>
                        {% if destinataire.notification %}
                            Oui
                        {% else %}
                            Non
                        {% endif %}
                    </td>
                </tr>
            </table>
        </div>
    </div>

    {{ include('@AcMarchePresse/destinataire/_delete_form.html.twig') }}
{% endblock %}
