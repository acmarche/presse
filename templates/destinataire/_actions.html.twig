<a href="{{ path('presse_destinataire_index') }}" class="btn btn-primary" role='button'><i class="ti ti-list"></i>
    Retour à la liste
</a>

<a href="{{ path('presse_destinataire_edit', { 'id': destinataire.id }) }}" role="button"
   class="btn btn-primary"><i class="ti ti-edit"></i> Editer</a>

<button class="btn btn-danger" role='button' data-toggle="modal" data-target="#deleteModal"><i
            class="ti ti-trash"></i>
    Supprimer
</button>

<br/>
<br/>