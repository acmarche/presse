<a href="{{ path('presse_user_index') }}" class="btn btn-primary" role='button'><i class="fas fa-list"></i>
    Retour à la liste
</a>

<a href="{{ path('presse_user_edit', { 'id': user.id }) }}" role="button"
   class="btn btn-primary"><i class="far fa-edit"></i> Editer</a>

<a href="{{ path('presse_user_password', { 'id': user.id }) }}" role="button"
   class="btn btn-primary"><i class="fas fa-lock"></i> Mot de passe</a>

<button class="btn btn-danger" role='button' data-toggle="modal" data-target="#deleteModal"><i
            class="fas fa-trash"></i>
    Supprimer
</button>

<br/>
<br/>